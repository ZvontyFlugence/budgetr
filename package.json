{
  "name": "client",
  "version": "0.1.0",
  "scripts": {
    "start": "concurrently \"cd client && npm run start\" \"cd server && python3 server.py\"",
    "build": "cd client && npm install --only=dev && npm install && npm run build",
    "test": "cd client && npm run test",
    "eject": "cd client && npm run eject"
  },
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
